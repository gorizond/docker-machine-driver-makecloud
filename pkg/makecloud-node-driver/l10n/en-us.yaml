cluster:
  provider:
    makecloud: MakeCloud
  credential:
    makecloud:
      actions:
        authenticate: Authenticate
        edit: Edit auth config
        addToAllowList: Add to allow list
      errors:
        unauthorized: Unauthorized (check token)
        fetchVdcs: Failed to fetch VDC list
        notAllowed: "{ hostname } is not present in Node Driver whitelistDomains."
      token:
        label: MakeCloud API token
        placeholder: xxxxxxxx...
      baseUrl:
        label: API base URL
        placeholder: https://cp.iteco.cloud
      insecureTLS:
        label: Skip TLS verification (insecure)
      vdcId:
        label: VDC
  machineConfig:
    makecloud:
      api:
        header: API
        token:
          label: API token
        baseUrl:
          label: API base URL
        insecureTLS:
          label: Skip TLS verification (insecure)
      vm:
        header: VM
        vdcId:
          label: VDC ID
        templateId:
          label: Template ID
        templateName:
          label: Template name
        storageProfileId:
          label: Storage profile ID
        networkId:
          label: Network ID
        allocateFloatingIp:
          label: Allocate a new public IP
        floatingIp:
          label: Floating IP (address or ID)
        firewallTemplateIds:
          label: Firewall templates
          tooltip: If empty, no firewall templates are applied (not recommended). For public IP, include a WEB template to allow inbound traffic.
      resources:
        header: Resources
        cpu:
          label: CPU cores
        ram:
          label: RAM (GiB)
        diskSize:
          label: Root disk size (GiB)
      ssh:
        header: SSH
        user:
          label: SSH user
        port:
          label: SSH port
      advanced:
        header: Advanced
        firewallTemplateIds:
          label: Firewall templates
          tooltip: If empty, defaults are applied (egress; WEB on floating IP)
        tags:
          label: Tags (repeatable)
        metadata:
          label: Template fields (key=value, repeatable)
        userData:
          label: Cloud-init user-data
        noInjectSshKey:
          label: Disable automatic SSH key injection
        waitTimeout:
          label: Wait timeout (seconds)
